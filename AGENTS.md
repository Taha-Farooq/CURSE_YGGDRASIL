# Agent context — CURSE repo

Use this file as the **onboarding anchor** for coding agents and humans: where truth lives, how quality gates work, and how backlog slices are executed.

## Product sources of truth

| Artifact | Role |
|----------|------|
| `REQUIREMENTS.md` | Product vision, guardrails, canon sections (e.g. precursor / reset cycle §38). |
| `backlog/tasks.json` | Canonical **`TASK-FEAT-*`** work items (full schema). Legacy **`TASK-*`** rows are REQUIREMENTS mirrors; they are **not** schema-validated the same way (see `scripts/validate-backlog-tasks.ps1`). |
| `backlog/epics.json` | Epic registry referenced by FEAT rows. |
| `backlog/development-slices.json` | **Execution order** for v1 vertical slices (`SLICE-V1-01` …); task definitions remain in `tasks.json`. Notes reference canonical **TASK-FEAT-061**–**148** follow-ups outside the governance union (see table below). |
| `docs/backlog/v1-plan-governance.md` | Dedupe, originality, and slice discipline. |
| `docs/backlog/design-gaps-policy-catalog.md` | Resolved policy/backlog mismatches and active follow-ups (precursor split, bot freshness). |

## V1 development slices (execution order)

Canonical task rows live in `backlog/tasks.json`; ordering is in `backlog/development-slices.json`. Union **023–060** must match slice membership for governance checks. **Current backlog:** all **`TASK-FEAT-023`–`060`** rows are **`done`** — new game vertical work extends **`061+`** follow-ups (**`126`**–**`131`** world-living tick vertical **done** through fleet deployment posture stub; **`132`** live tick host cluster / RPC / deploy **todo**; **`133`** battle encounter directive golden fixture **done**; **`134`** battle named arc + live-ops fixture chain + cross-validator **done**; **`135`** battle directive runtime **posture stub** **done**; **`136`** live directive executor / stores / audit **todo**; **`137`** dungeon run authority **posture stub** **done**; **`138`** craft station authority **posture stub** **done**; **`139`** dungeon live run authority executor **todo**; **`140`** craft station live authority executor **todo**; **`141`** summon continuity authority **posture stub** **done**; **`142`** summon continuity live authority executor **todo**; **`143`** combat resolution authority **posture stub** **done**; **`144`** combat resolution live authority executor **todo**; **`145`** fluids band authority **posture stub** **done**; **`146`** fluids band live authority executor **todo**; **`147`** presentation scale authority **posture stub** **done**; **`148`** presentation scale live authority executor **todo**), not by silently reopening closed slice rows.

| Slice | Focus | Task ids (primary) |
|-------|--------|---------------------|
| SLICE-V1-01 | Backlog governance | `TASK-FEAT-048` |
| SLICE-V1-02 | Living world (v1 contracts **done**) | `023`, `032`, `033` |
| SLICE-V1-03 | Presentation / Vulkan / mega-scale (`024`–`027` **done**; systems posture **`147`** **done**; live render executor **`148`** **todo**) | `024`, `026`, `027` |
| SLICE-V1-04 | Fluids, naval, atmosphere (`025` **done**; systems posture **`145`** **done**; live band executor **`146`** **todo**) | `025` |
| SLICE-V1-05 | Battle AI, named NPC lifecycle, live-ops hooks (`028` **done**; fixtures **`133`**–**`134`** **done**; posture **`135`** **done**; live executor **`136`** **todo**) | `028` |
| SLICE-V1-06 | Combat identity, melee, war UX, perspectives, RTS unlock (`029`–`042` **done**; systems posture **`143`** **done**; live resolution executor **`144`** **todo**) | `029`, `037`, `038`, `041`, `042` |
| SLICE-V1-07 | Summons, resurrection, reincarnation, death/recovery magic (`030`–`036` **done**; systems posture **`141`** **done**; live continuity executor **`142`** **todo**) | `030`, `031`, `035`, `036` |
| SLICE-V1-08 | Dungeon autonomy, dungeon master, persistence vs collapse (`034` **done**; systems posture **`137`** **done**; live run executor **`139`** **todo**) | `034` |
| SLICE-V1-09 | Crafting, forging, artisan mastery (`043`–`046` **done**; systems posture **`138`** **done**; live station executor **`140`** **todo**) | `043`, `044`, `046` |
| SLICE-V1-10 | Appendix mechanic batches A–C, Overlord-tone feature catalog | `039`, `040`, `045`, `047` |
| SLICE-V1-11 | Tooling, observability, investigator harnesses | `049`–`060` (`engine/ops/tooling-harness-v1/`; **done**) |
| SLICE-V1-12 | Precursor canon, evidence ledger, narrative incident feed | `061`, `062`, `063`, `006` (`engine/sim/world-history-v1/`; **done**) |
| SLICE-V1-13 | Unified tooling cockpit shell over harness vertical | `072` (`engine/ops/unified-tooling-cockpit-v1/`; **done**) |
| SLICE-V1-14 | Observability vendor posture (pilots, residency, rollback) | `073` (`engine/ops/observability-vendor-posture-v1/`; **done**) |
| SLICE-V1-15 | Archival / cinematic / ML governance posture (world history) | `074` (`engine/sim/world-history-v1/archival-canon-posture-v1/`; **done**) |
| SLICE-V1-16 | Realtime operator plane (optional push) over cockpit shell | `075` (`engine/ops/unified-tooling-cockpit-v1/realtime-operator-plane-v1/`; **done**) |
| SLICE-V1-17 | Observability vendor integration manifest (adapter stub, rollout checklist) | `076` (`engine/ops/observability-vendor-integration-v1/`; **done**) |
| SLICE-V1-18 | Archival production pilot (index/cinema/ML stub contracts) | `077` (`engine/sim/world-history-v1/archival-production-pilot-v1/`; **done**) |
| SLICE-V1-19 | Enterprise observability operations (escalation, on-call, failover drills) | `078` (`engine/ops/enterprise-observability-ops-v1/`; **done**) |
| SLICE-V1-20 | Archival hyperscale posture (index topology, render pool, ML hosting boundary stubs) | `079` (`engine/sim/world-history-v1/archival-hyperscale-posture-v1/`; **done**) |
| SLICE-V1-21 | Live paging / MSSP handoff contract stubs (no live URLs) | `080` (`engine/ops/live-paging-mssp-contract-v1/`; **done**) |
| SLICE-V1-22 | Continuity ops lane index (cockpit → observability → paging chain map) | `083` (`engine/ops/continuity-ops-lane-v1/`; **done**) |
| SLICE-V1-23 | Cockpit continuity slot registry (optional lane-aligned slots, fail-open) | `084` (`engine/ops/unified-tooling-cockpit-v1/continuity-adapters-v1/`; **done**) |
| SLICE-V1-24 | Cockpit continuity runtime binding (bot-report globs, drill-down policy, cross-refs) | `085` (`continuity-runtime-binding-stub-v1.json`; **done**) |
| SLICE-V1-25 | Cockpit continuity dashboard (read-only API + control panel card) | `086` (`dashboard/index.html`, `GET /api/continuity-cockpit`; **done**) |
| SLICE-V1-26 | Cockpit continuity live stream manifest (env contract + API surface) | `087` (`continuity-live-stream-manifest-stub-v1.json`, `GET /api/continuity-stream-manifest`; **done**) |
| SLICE-V1-27 | Cockpit continuity dev SSE gateway (local smoke stream) | `088` (`GET /api/continuity-soc-events`, `CURSE_CONTINUITY_SSE_ENABLE`; **done**) |
| SLICE-V1-28 | Cockpit continuity production gateway contract (env topology stub + APIs) | `089` (`continuity-production-wss-gateway-contract-stub-v1.json`, `GET /api/continuity-production-gateway-contract`; **done**) |
| SLICE-V1-29 | Cockpit continuity gateway deployment posture stub (K8s names, probes, image env) | `090` (`continuity-gateway-service-deployment-stub-v1.json`, `GET /api/continuity-gateway-service-deployment`; **done**) |
| SLICE-V1-30 | Cockpit continuity gateway container pack (Dockerfile, Helm scaffold, stub process) | `091` (`engine/ops/continuity-gateway-pack-v1/`, `continuity-gateway-container-pack-stub-v1.json`, `GET /api/continuity-gateway-container-pack`; **done**) |
| SLICE-V1-31 | Cockpit continuity gateway CI workflow + go-live posture stub | `092` (`.github/workflows/continuity-gateway-pack.yml`, `continuity-gateway-golive-posture-stub-v1.json`, `GET /api/continuity-gateway-golive-posture`; **done**) |
| SLICE-V1-32 | Cockpit continuity gateway cluster multiplex posture stub | `093` (`continuity-gateway-cluster-multiplex-posture-stub-v1.json`, optional Helm ingress off by default, `production-rollout-checklist-v1.md`, `GET /api/continuity-gateway-cluster-multiplex-posture`; **done**) |
| SLICE-V1-33 | Cockpit continuity gateway operational soak (metrics, HPA, registry hints) | `094` (`continuity-gateway-operational-soak-stub-v1.json`, `server.mjs` `/metrics` + multiplex echo, optional Helm HPA off by default, `GET /api/continuity-gateway-operational-soak`; **done**) |
| SLICE-V1-34 | Cockpit continuity gateway hosted integration posture (CI docs, optional GHCR, MSSP checklist pointers) | `095` (`continuity-gateway-hosted-integration-posture-stub-v1.json`, `GET /api/continuity-gateway-hosted-integration`; **done**) |
| SLICE-V1-35 | Cockpit continuity gateway tenant operations posture (OIDC hints, remote-state hints, MSSP evidence envelope stub) | `096` (`continuity-gateway-tenant-operations-posture-stub-v1.json`, `GET /api/continuity-gateway-tenant-operations`; **done**) |
| SLICE-V1-36 | Cockpit continuity gateway live delivery posture (rollout checklist, CI integration-test contract names, MSSP vault handoff env names, JSON smoke in CI) | `097` (`continuity-gateway-live-delivery-posture-stub-v1.json`, `scripts/validate-continuity-gateway-json-smoke.ps1`, `GET /api/continuity-gateway-live-delivery`; **done**) |
| SLICE-V1-37 | Cockpit continuity gateway tenant-bound posture (CI pipeline hints, Terraform fmt/validate stub, cockpit API) | `098` (`continuity-gateway-tenant-bound-posture-stub-v1.json`, `terraform/stub/ci-smoke`, `GET /api/continuity-gateway-tenant-bound`; **stub shipped**, live OIDC apply / vault **todo**) |
| SLICE-V1-38 | Multiregion mesh / SOAR overflow stub + checklist (parity with manifest vertical chain) | `099` (`continuity-multiregion-mesh-overflow-stub-v1.json`, `docs/multiregion-mesh-overflow-checklist-v1.md`, `GET /api/continuity-gateway-multiregion-mesh-overflow`; **stub shipped**); live mesh / vendor SOAR / hosted SOC remains **`099`** (**todo**) |
| SLICE-V1-39 | Compliance attestation posture stub + checklist + cockpit route | `100` (`continuity-compliance-attestation-posture-stub-v1.json`, `docs/compliance-attestation-checklist-v1.md`, `GET /api/continuity-gateway-compliance-attestation`; **stub shipped**); live SIEM/GRC pipelines remain **`100`** (**todo**) |
| SLICE-V1-40 | External audit portal overflow stub + checklist + cockpit route | `101` (`continuity-external-audit-portal-overflow-stub-v1.json`, `docs/external-audit-portal-overflow-checklist-v1.md`, `GET /api/continuity-gateway-external-audit-portal-overflow`; **stub shipped**); live IdP federation / trust centers remain **`101`** (**todo**) |
| SLICE-V1-41 | Cross-border regulatory evidence overflow stub + checklist + cockpit route | `102` (`continuity-cross-border-regulatory-evidence-overflow-stub-v1.json`, `docs/cross-border-regulatory-evidence-overflow-checklist-v1.md`, `GET /api/continuity-gateway-cross-border-regulatory-evidence-overflow`; **stub shipped**); live cross-border programs remain **`102`** (**todo**) |
| SLICE-V1-42 | Industry vertical regulator overflow stub + checklist + cockpit route | `103` (`continuity-industry-vertical-regulator-overflow-stub-v1.json`, `docs/industry-vertical-regulator-overflow-checklist-v1.md`, `GET /api/continuity-gateway-industry-vertical-regulator-overflow`; **stub shipped**); live sector programs remain **`103`** (**todo**) |
| SLICE-V1-43 | Sovereign data localization overflow stub + checklist + cockpit route | `104` (`continuity-sovereign-data-localization-overflow-stub-v1.json`, `docs/sovereign-data-localization-overflow-checklist-v1.md`, `GET /api/continuity-gateway-sovereign-data-localization-overflow`; **stub shipped**); live residency / lawful routing remain **`104`** (**todo**) |
| SLICE-V1-44 | Regulatory filing calendar overflow stub + checklist + cockpit route | `105` (`continuity-regulatory-filing-calendar-overflow-stub-v1.json`, `docs/regulatory-filing-calendar-overflow-checklist-v1.md`, `GET /api/continuity-gateway-regulatory-filing-calendar-overflow`; **stub shipped**); live GRC calendars / portal automation remain **`105`** (**todo**) |
| SLICE-V1-45 | CCM / regulator examination overflow stub + checklist + cockpit route | `106` (`continuity-ccm-regulator-examination-overflow-stub-v1.json`, `docs/ccm-regulator-examination-overflow-checklist-v1.md`, `GET /api/continuity-gateway-ccm-regulator-examination-overflow`; **stub shipped**); live CCM / examiner coordination remain **`106`** (**todo**); live supervisory remediation / enforcement programs **`107`** (**todo**) |
| SLICE-V1-46 | Supervisory remediation / enforcement overflow stub + checklist + cockpit route | `107` (`continuity-supervisory-remediation-enforcement-overflow-stub-v1.json`, `docs/supervisory-remediation-enforcement-overflow-checklist-v1.md`, `GET /api/continuity-gateway-supervisory-remediation-enforcement-overflow`; **stub shipped**); live enforcement / MRAs / consent orders remain **`107`** (**todo**) |
| SLICE-V1-47 | Resolution planning / crisis supervisory overflow stub + checklist + cockpit route | `108` (`continuity-resolution-planning-crisis-supervisory-overflow-stub-v1.json`, `docs/resolution-planning-crisis-supervisory-overflow-checklist-v1.md`, `GET /api/continuity-gateway-resolution-planning-crisis-supervisory-overflow`; **stub shipped**); live crisis theater remains **`108`** (**todo**) |
| SLICE-V1-48 | Cross-border insolvency restructuring forum overflow stub + checklist + cockpit route | `109` (`continuity-cross-border-insolvency-restructuring-forum-overflow-stub-v1.json`, `docs/cross-border-insolvency-restructuring-forum-overflow-checklist-v1.md`, `GET /api/continuity-gateway-cross-border-insolvency-restructuring-forum-overflow`; **stub shipped**); live forums / tribunals remain **`109`** (**todo**) |
| SLICE-V1-49 | Creditor hierarchy / deposit-insurance bridge overflow stub + checklist + cockpit route | `110` (`continuity-creditor-hierarchy-deposit-insurance-bridge-overflow-stub-v1.json`, `docs/creditor-hierarchy-deposit-insurance-bridge-overflow-checklist-v1.md`, `GET /api/continuity-gateway-creditor-hierarchy-deposit-insurance-bridge-overflow`; **stub shipped**); live payout programs remain **`110`** (**todo**) |
| SLICE-V1-50 | Resolution funding / liquidity backstop overflow stub + checklist + cockpit route | `111` (`continuity-resolution-funding-liquidity-backstop-overflow-stub-v1.json`, `docs/resolution-funding-liquidity-backstop-overflow-checklist-v1.md`, `GET /api/continuity-gateway-resolution-funding-liquidity-backstop-overflow`; **stub shipped**); live treasury / liquidity facilities remain **`111`** (**todo**) |
| SLICE-V1-51 | Systemic risk capital buffer / recovery-planning overflow stub + checklist + cockpit route | `112` (`continuity-systemic-risk-capital-buffer-recovery-planning-overflow-stub-v1.json`, `docs/systemic-risk-capital-buffer-recovery-planning-overflow-checklist-v1.md`, `GET /api/continuity-gateway-systemic-risk-capital-buffer-recovery-planning-overflow`; **stub shipped**); live macro-prudential programs remain **`112`** (**todo**) |
| SLICE-V1-52 | Digital operational resilience / critical third-party overflow stub + checklist + cockpit route | `113` (`continuity-digital-operational-resilience-critical-third-party-overflow-stub-v1.json`, `docs/digital-operational-resilience-critical-third-party-overflow-checklist-v1.md`, `GET /api/continuity-gateway-digital-operational-resilience-critical-third-party-overflow`; **stub shipped**); live ICT/vendor programs remain **`113`** (**todo**) |
| SLICE-V1-53 | Nested outsourcing / fourth-party concentration overflow stub + checklist + cockpit route | `114` (`continuity-nested-outsourcing-fourth-party-concentration-overflow-stub-v1.json`, `docs/nested-outsourcing-fourth-party-concentration-overflow-checklist-v1.md`, `GET /api/continuity-gateway-nested-outsourcing-fourth-party-concentration-overflow`; **stub shipped**); live nested-outsourcing / fourth-party programs remain **`114`** (**todo**) |
| SLICE-V1-54 | Supply-chain cascade / substitutability matrix overflow stub + checklist + cockpit route | `115` (`continuity-supply-chain-cascade-substitutability-matrix-overflow-stub-v1.json`, `docs/supply-chain-cascade-substitutability-matrix-overflow-checklist-v1.md`, `GET /api/continuity-gateway-supply-chain-cascade-substitutability-matrix-overflow`; **stub shipped**); live matrices and proofs remain **`115`** (**todo**) |
| SLICE-V1-55 | Geopolitical supply disruption / sanctions escalation overflow stub + checklist + cockpit route | `116` (`continuity-geopolitical-supply-disruption-sanctions-escalation-overflow-stub-v1.json`, `docs/geopolitical-supply-disruption-sanctions-escalation-overflow-checklist-v1.md`, `GET /api/continuity-gateway-geopolitical-supply-disruption-sanctions-escalation-overflow`; **stub shipped**); live sanctions lists and screening scores remain **`116`** (**todo**); counterparty screening overflow stub **`117`** (**stub shipped**; live engines **`117`** **todo**) |
| SLICE-V1-56 | Export control dual-use commodity routing overflow stub + checklist + cockpit route | `118` (`continuity-export-control-dual-use-commodity-routing-overflow-stub-v1.json`, `docs/export-control-dual-use-commodity-routing-overflow-checklist-v1.md`, `GET /api/continuity-gateway-export-control-dual-use-commodity-routing-overflow`; **stub shipped**); sealed routing attestations and matrices remain **`118`** (**todo**). |
| SLICE-V1-57 | End-user certificate / destination control overflow stub + checklist + cockpit route | `119` (`continuity-end-user-certificate-destination-control-program-overflow-stub-v1.json`, `docs/end-user-certificate-destination-control-program-overflow-checklist-v1.md`, `GET /api/continuity-gateway-end-user-certificate-destination-control-program-overflow`; **stub shipped**); sealed certificate bodies and live EUC programs remain **`119`** (**todo**). |
| SLICE-V1-58 | Trade finance documentary compliance overflow stub + checklist + cockpit route | `120` (`continuity-trade-finance-documentary-compliance-overflow-stub-v1.json`, `docs/trade-finance-documentary-compliance-overflow-checklist-v1.md`, `GET /api/continuity-gateway-trade-finance-documentary-compliance-overflow`; **stub shipped**); live LC / documentary / bank sealed filings remain **`120`** (**todo**). |
| SLICE-V1-59 | Cross-border correspondent banking / payment-rail overflow stub + checklist + cockpit route | `121` (`continuity-cross-border-correspondent-banking-payment-rail-overflow-stub-v1.json`, `docs/cross-border-correspondent-banking-payment-rail-overflow-checklist-v1.md`, `GET /api/continuity-gateway-cross-border-correspondent-banking-payment-rail-overflow`; **stub shipped**); live correspondent attestations remain **`121`** (**todo**). |
| SLICE-V1-60 | Instant payment / domestic RTGS liquidity-bridge overflow stub + checklist + cockpit route | `122` (`continuity-instant-payment-and-rtgs-liquidity-bridge-overflow-stub-v1.json`, `docs/instant-payment-and-rtgs-liquidity-bridge-overflow-checklist-v1.md`, `GET /api/continuity-gateway-instant-payment-and-rtgs-liquidity-bridge-overflow`; **stub shipped**); live instant-rail bodies remain **`122`** (**todo**). |
| SLICE-V1-61 | Tokenized deposit / stablecoin / wholesale MM overflow stub + checklist + cockpit route | `123` (`continuity-tokenized-deposit-stablecoin-wholesale-money-market-overflow-stub-v1.json`, `docs/tokenized-deposit-stablecoin-wholesale-money-market-overflow-checklist-v1.md`, `GET /api/continuity-gateway-tokenized-deposit-stablecoin-wholesale-money-market-overflow`; **stub shipped**); live sealed token/MM bodies remain **`123`** (**todo**). |
| SLICE-V1-62 | Programmable money / DVP / CCP-style settlement rail overflow stub + checklist + cockpit route | `124` (`continuity-programmable-money-dvp-settlement-rail-overflow-stub-v1.json`, `docs/programmable-money-dvp-settlement-rail-overflow-checklist-v1.md`, `GET /api/continuity-gateway-programmable-money-dvp-settlement-rail-overflow`; **stub shipped**); live DVP / netting bodies remain **`124`** (**todo**). |
| SLICE-V1-63 | Wholesale CBDC bridge / cross-venue interoperability overflow stub + checklist + cockpit route | `125` (`continuity-wholesale-cbdc-bridge-interop-overflow-stub-v1.json`, `docs/wholesale-cbdc-bridge-interop-overflow-checklist-v1.md`, `GET /api/continuity-gateway-wholesale-cbdc-bridge-interop-overflow`; **stub shipped**); live bridge proofs / interop matrices remain **`125`** (**todo**). |
| SLICE-V1-64 | World living tick vertical through fleet posture stub (live host deferred) | `126`–`131` (**done**), `132` (**todo** live cluster / RPC / deploy) |
| SLICE-V1-65 | Battle / named NPC AI fixture vertical (post-028 contracts) | `133`–`135` (**done** golden JSON + cross-fixtures + directive runtime posture stub) |
| SLICE-V1-66 | Battle directive live executor integration (overflow past posture stub) | `136` (**todo** in-process executor, live knob stores, audit sink) |
| SLICE-V1-67 | Dungeon run authority systems posture (post-034 fixtures) | `137` (**done** posture stub + strict policy) |
| SLICE-V1-68 | Dungeon live run authority executor (overflow past posture stub) | `139` (**todo** shard executor, collapse stores, replay export) |
| SLICE-V1-69 | Craft station authority systems posture (post-043 fixtures) | `138` (**done** posture stub + strict policy) |
| SLICE-V1-70 | Craft station live authority executor (overflow past posture stub) | `140` (**todo** shard executor, recipe graph store, replay export) |
| SLICE-V1-71 | Summon continuity authority systems posture (post-030 fixtures) | `141` (**done** posture stub + strict policy) |
| SLICE-V1-72 | Summon continuity live authority executor (overflow past posture stub) | `142` (**todo** shard executor, graph edge store, replay export) |
| SLICE-V1-73 | Combat resolution authority systems posture (post-029 fixtures) | `143` (**done** posture stub + strict policy) |
| SLICE-V1-74 | Combat resolution live authority executor (overflow past posture stub) | `144` (**todo** shard executor, style-ledger timeline store, replay export) |
| SLICE-V1-75 | Fluids band authority systems posture (post-025 fixtures) | `145` (**done** posture stub + strict policy) |
| SLICE-V1-76 | Fluids band live authority executor (overflow past posture stub) | `146` (**todo** shard executor, hydro transition ledger, replay export) |
| SLICE-V1-77 | Presentation scale authority systems posture (post-027 fixtures) | `147` (**done** posture stub + strict policy) |
| SLICE-V1-78 | Presentation scale live authority executor (overflow past posture stub) | `148` (**todo** shard executor, cohort envelope ledger, swapchain coordination, replay export) |

## Where to work next

- **Game verticals:** governance union **`023`–`060`** is **complete** in `tasks.json`. Extend the sim with **`TASK-FEAT-061+`** (precursor, deferred GPU/CFD/ML rows, etc.); world-living **`126`**–**`131`** (contracts + cross-fixture CI + fleet deployment posture stub) is **done**; pick up **`132`** for live tick host cluster / transports / deploy; battle **`133`**–**`135`** is **done** — pick up **`136`** for live battle executor; dungeon **`137`** is **done** — pick up **`139`** for live dungeon run shard (distinct from **`069`** LLM DM); craft **`138`** posture is **done** — pick up **`140`** for live craft station executor (recipe graph / replay); summon continuity **`141`** posture is **done** — pick up **`142`** for live summon continuity executor (graph edges / replay); combat resolution **`143`** posture is **done** — pick up **`144`** for live combat resolution executor (style-ledger / replay); fluids band **`145`** posture is **done** — pick up **`146`** for live hydro band executor (transition ledger / replay); full CFD shoreline remains **`065`**; presentation scale **`147`** posture is **done** — pick up **`148`** for live render authority executor (cohort ledger / swapchain coordination / replay); advanced GPU remains **`064`**; cross-shard soulbind remains **`068`**; AAA RTS motion remains **`067`**; ultra-fidelity craft remains **`070`**; pick up **`132`**, **`142`**, **`144`**, **`146`**, **`148`**, **`SLICE-V1-03`**, **`SLICE-V1-04`**, or add FEAT rows after governance review — do not assume **`SLICE-V1-02`** is still the active blocker.  
- **Continuity repo stubs:** chain **`086`**–**`125`** is **stub-complete**; live execution remains on **`098`** (tenant-bound apply), **`082`** (paging/MSSP go-live), and **`099`**–**`125`** **todo** rows as marked in the slice table and `tasks.json`.  
- **Production timeline artifact:** `scripts/update-production-timeline.ps1` writes `reports/production-timeline-latest.json` (and `.md`); optional Cursor canvas: `canvases/production-timeline.canvas.tsx` beside the chat.

**Governance note:** `development-slices.json` intentionally lists **only** **`TASK-FEAT-023`–`060`** so `check-backlog-governance-policy.ps1` can validate the union. **`SLICE-V1-12`** and later IDs are **documentation-only** execution labels in this table; canonical rows remain in `tasks.json`. Precursor assets: `engine/sim/world-history-v1/`, `systems/world-history/fixtures/`. World-living contract fixtures: `systems/world-living/fixtures/`; tick-runtime README: `engine/sim/world-living-v1/tick-runtime-v1/`; shard lease README: `engine/sim/world-living-v1/tick-runtime-v1/shard-authority-v1/`; tick-host stub README: `engine/sim/world-living-v1/tick-runtime-v1/tick-host-stub-v1/`; tick production posture README: `engine/sim/world-living-v1/tick-runtime-v1/tick-production-posture-v1/`; tick host executable stub README: `engine/sim/world-living-v1/tick-runtime-v1/tick-host-executable-stub-v1/`; tick host fleet stub README: `engine/sim/world-living-v1/tick-runtime-v1/tick-host-fleet-stub-v1/`; cross-fixture script: `scripts/validate-world-tick-host-cross-fixtures.ps1`. Battle/NPC AI golden fixtures: `systems/battle-npc-ai/fixtures/` (includes `battle-directive-runtime-posture-stub-v1.json` **`135`**); directive runtime posture README: `engine/sim/battle-npc-ai-v1/directive-runtime-posture-stub-v1/`; battle contracts policy: `scripts/check-battle-npc-ai-contracts-policy.ps1`; battle cross-fixture harness: `scripts/validate-battle-npc-ai-cross-fixtures.ps1`. Dungeon autonomy systems fixtures: `systems/dungeon-autonomy/fixtures/` (`dungeon-run-authority-posture-stub-v1.json` **`137`**); dungeon posture README: `engine/sim/dungeon-autonomy-v1/dungeon-run-posture-stub-v1/`; dungeon contracts policy: `scripts/check-dungeon-autonomy-contracts-policy.ps1`. Craft / forge systems fixtures: `systems/craft-forge/fixtures/` (`craft-station-authority-posture-stub-v1.json` **`138`**); craft posture README: `engine/sim/craft-forge-v1/craft-station-posture-stub-v1/`; craft contracts policy: `scripts/check-craft-forge-contracts-policy.ps1`. Summon / mortality systems fixtures: `systems/summon-mortality/fixtures/` (`summon-continuity-authority-posture-stub-v1.json` **`141`**); summon continuity posture README: `engine/sim/summon-mortality-v1/summon-continuity-posture-stub-v1/`; summon contracts policy: `scripts/check-summon-mortality-contracts-policy.ps1`. Combat style systems fixtures: `systems/combat-style/fixtures/` (`combat-resolution-authority-posture-stub-v1.json` **`143`**); combat resolution posture README: `engine/combat/combat-style-v1/combat-resolution-posture-stub-v1/`; combat style contracts policy: `scripts/check-combat-style-contracts-policy.ps1`. Fluids / atmosphere systems fixtures: `systems/fluids-atmosphere/fixtures/` (`fluids-band-authority-posture-stub-v1.json` **`145`**); fluids band posture README: `engine/sim/fluids-atmosphere-v1/fluids-band-posture-stub-v1/`; fluids contracts policy: `scripts/check-fluids-atmosphere-contracts-policy.ps1`. Presentation scale systems fixtures: `systems/presentation-scale/fixtures/` (`presentation-scale-authority-posture-stub-v1.json` **`147`**); posture README: `engine/render/presentation-scale-authority-posture-stub-v1/`; render vertical index: `engine/render/README.md`; presentation scale contracts policy: `scripts/check-presentation-scale-contracts-policy.ps1`. **`072`** cockpit shell is documented here only; it does not expand the governance union.

## Quality gate

- Entry point: `scripts/quality-gate.ps1` (use **`-Strict`** before claiming green).  
- **After backlog or continuity JSON edits**, run from repo root (PowerShell):  
  `.\scripts\validate-backlog-tasks.ps1 -Strict`  
  `.\scripts\quality-gate.ps1 -Strict`  
  `.\scripts\validate-continuity-gateway-json-smoke.ps1`  
- World-living tick golden corpus cross-check runs inside **`.\scripts\check-foundational-world-systems-policy.ps1 -Strict`** via **`.\scripts\validate-world-tick-host-cross-fixtures.ps1`** (**`130`** **done**); fleet posture stub README + JSON keys are enforced in the same script (**`131`** **done**).  
- Battle / named NPC AI fixture keys and cross-consistency run under **`.\scripts\check-battle-npc-ai-contracts-policy.ps1 -Strict`** (includes **`.\scripts\validate-battle-npc-ai-cross-fixtures.ps1`**; **`133`**–**`135`** **done**); live executor / stores remain **`136`**.  
- Dungeon autonomy posture fixture keys run under **`.\scripts\check-dungeon-autonomy-contracts-policy.ps1 -Strict`** (**`137`** **done**).  
- Craft / forge station posture fixture keys run under **`.\scripts\check-craft-forge-contracts-policy.ps1 -Strict`** (**`138`** **done**).  
- Summon / mortality continuity posture fixture keys run under **`.\scripts\check-summon-mortality-contracts-policy.ps1 -Strict`** (**`141`** **done**); live continuity executor / graph edge stores remain **`142`**.  
- Combat style resolution posture fixture keys run under **`.\scripts\check-combat-style-contracts-policy.ps1 -Strict`** (**`143`** **done**); live resolution executor / style-ledger timeline stores remain **`144`**.  
- Fluids / atmosphere band posture fixture keys run under **`.\scripts\check-fluids-atmosphere-contracts-policy.ps1 -Strict`** (**`145`** **done**); live band executor / hydro transition ledger stores remain **`146`** (full CFD **`065`**).  
- Presentation / Vulkan / mega-scale posture fixture keys run under **`.\scripts\check-presentation-scale-contracts-policy.ps1 -Strict`** (**`147`** **done**); live presentation-scale executor / cohort envelope ledger / swapchain coordination remain **`148`** (advanced GPU **`064`**).  
- Continuity frontier: **`097`** live delivery posture **done**. **`098`** ships tenant-bound stub JSON, **`terraform/stub/ci-smoke`** validation in CI, **`GET /api/continuity-gateway-tenant-bound`** — credential-backed **`terraform apply`**, integration tests, and MSSP uploads remain **open** under **`098`**. **`099`** repo-safe **overflow stub** + checklist **shipped**; **live** mesh / **SOAR** / **hosted SOC** remain **`099` todo**. **`100`** repo-safe **compliance attestation posture stub** + checklist **`shipped`**; **live** SIEM/GRC pipelines remain **`100` todo**. **`101`** repo-safe **external audit portal overflow stub** + checklist **`shipped`**; **live** IdP / trust-center integration remains **`101` todo**. **`102`** repo-safe **cross-border regulatory evidence overflow stub** + checklist **`shipped`**; **live** clearinghouse / MOU execution remains **`102` todo**. **`103`** repo-safe **industry vertical regulator overflow stub** + checklist **`shipped`**; **live** sector enrollment remains **`103` todo**. **`104`** repo-safe **sovereign data localization overflow stub** + checklist **`shipped`**; **live** residency / KMS / lawful programs remain **`104` todo**. **`105`** repo-safe **regulatory filing calendar overflow stub** + checklist **`shipped`**; **live** GRC calendars and portal submissions remain **`105` todo**. **`106`** repo-safe **CCM / regulator examination overflow stub** + checklist **`shipped`**; **live** continuous control monitoring and regulator examinations remain **`106` todo**. **`107`** repo-safe **supervisory remediation / enforcement overflow stub** + checklist **`shipped`**; **live** supervisory remediation and enforcement programs remain **`107` todo**. **`108`** repo-safe **resolution planning / crisis supervisory overflow stub** + checklist **`shipped`**; **live** crisis theater / sealed consent programs remain **`108` todo**. **`109`** repo-safe **cross-border insolvency restructuring forum overflow stub** + checklist **`shipped`**; **live** forums / tribunals remain **`109` todo**. **`110`** repo-safe **creditor hierarchy / deposit-insurance bridge overflow stub** + checklist **`shipped`**; **live** payout programs remain **`110` todo**. **`111`** repo-safe **resolution funding / liquidity backstop overflow stub** + checklist **`shipped`**; **live** treasury facilities remain **`111` todo**. **`112`** repo-safe **systemic risk capital buffer / recovery-planning overflow stub** + checklist **`shipped`**; **live** macro-prudential programs remain **`112` todo**. **`113`** repo-safe **digital operational resilience / critical third-party overflow stub** + checklist **`shipped`**; **live** ICT/vendor programs remain **`113` todo**. **`114`** repo-safe **nested outsourcing / fourth-party concentration overflow stub** + checklist **`shipped`**; **live** nested-outsourcing programs remain **`114` todo**. **`115`** repo-safe **supply-chain cascade / substitutability matrix overflow stub** + checklist **`shipped`**; **live** matrices and proofs remain **`115` todo**. **`116`** repo-safe **geopolitical supply disruption / sanctions escalation overflow stub** + checklist **`shipped`**; **live** sanctions programs remain **`116` todo**. **`117`** repo-safe **counterparty sanctions screening program overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-counterparty-sanctions-screening-program-overflow`**); **live** screening engines remain **`117` todo**. **`118`** repo-safe **export control dual-use commodity routing overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-export-control-dual-use-commodity-routing-overflow`**); **live** sealed attestations / matrices remain **`118` todo**; **`exportControlDualUseCommodityRoutingAttestationDeferred`** stays for sealed programs. **`119`** repo-safe **end-user certificate destination control overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-end-user-certificate-destination-control-program-overflow`**); **live** sealed certificate / EUC bodies remain **`119` todo**; **`endUserCertificateDestinationControlProgramDeferred`** stays for sealed programs; **`tradeFinanceDocumentaryComplianceOverflowStub`** chains documentary trade hints to **`120`**. **`120`** repo-safe **trade finance documentary compliance overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-trade-finance-documentary-compliance-overflow`**); **live** LC / documentary / bank sealed filings remain **`120` todo**; **`crossBorderCorrespondentBankingAndPaymentRailAttestationDeferred`** → **`121`**. **`121`** repo-safe **cross-border correspondent banking payment-rail overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-cross-border-correspondent-banking-payment-rail-overflow`**); **live** correspondent attestations remain **`121` todo**; **`instantPaymentAndRtgsLiquidityBridgeAttestationDeferred`** → **`122`**. **`122`** repo-safe **instant payment / RTGS liquidity-bridge overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-instant-payment-and-rtgs-liquidity-bridge-overflow`**); **live** instant-rail bodies remain **`122` todo**; **`tokenizedDepositStablecoinAndWholesaleMoneyMarketAttestationDeferred`** → **`123`**. **`123`** repo-safe **tokenized deposit / stablecoin / wholesale MM overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-tokenized-deposit-stablecoin-wholesale-money-market-overflow`**); **live** sealed token/MM bodies remain **`123` todo**; **`programmableMoneyAndDvpSettlementRailAttestationDeferred`** → **`124`**. **`124`** repo-safe **programmable money / DVP / CCP-style settlement rail overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-programmable-money-dvp-settlement-rail-overflow`**); **live** DVP / netting bodies remain **`124` todo**; **`cbdcBridgeAndInteropAttestationDeferred`** → **`125`**. **`125`** repo-safe **wholesale CBDC bridge / interoperability overflow stub** + checklist **`shipped`** (**`GET /api/continuity-gateway-wholesale-cbdc-bridge-interop-overflow`**); **live** bridge proofs / interop matrices remain **`125` todo**; cockpit snapshot **`taskFeatRef` `125`** aggregates the vertical through **`125`**.
- Backlog: `validate-backlog-tasks.ps1` validates **`TASK-FEAT-*`** rows only; legacy `TASK-*` entries are ignored for schema checks but must keep **unique ids**.  
- Duplicates: `check-task-duplicates.ps1 -Strict`.  
- Governance union: `check-backlog-governance-policy.ps1` ensures `development-slices.json` references exactly the **`TASK-FEAT-023`–`060`** rows (additional FEAT ids **061+** are **follow-ups**: precursor **061–063** (+ **`006`** integration), deferred GPU **064**, CFD **065**, ML/personality **066**, AAA RTS/motion **067**, cross-shard summon soulbind **068**, LLM/procedural dungeon DM **069**, ultra-fidelity crafting **070**, full appendix runtime **071**, unified tooling cockpit shell **072** (**done**), observability vendor posture **073** (**done**), archival/cinematic posture **074** (**done**), realtime operator plane **075** (**done**), vendor integration manifest **076** (**done**), archival production pilot **077** (**done**), enterprise observability operations **078** (**done**), archival hyperscale posture **079** (**done**), live paging / MSSP contract stubs **080** (**done**), archival hyperscale **go-live** **081**, live paging **go-live** **082**, continuity ops lane index **083** (**done**), cockpit continuity slot registry **084** (**done**), cockpit continuity **runtime binding** **085** (**done**), cockpit continuity **dashboard** read-only **086** (**done**), cockpit continuity **live stream manifest** **087** (**done**), continuity **dev SSE gateway** **088** (**done**), production **gateway contract** **089** (**done**), gateway **deployment posture stub** **090** (**done**), gateway **container pack** **091** (**done**), gateway **CI workflow + go-live posture** **092** (**done**), gateway **cluster multiplex posture** **093** (**done**), gateway **operational soak** **094** (**done**), hosted **integration posture** **095** (**done**), tenant **operations posture** **096** (**done**), **live delivery posture** **097** (**done**), **tenant-bound** IaC / MSSP vault **098** (**todo**; stub + CI terraform smoke shipped), **multi-region mesh / SOAR / hosted SOC** **099** (**todo** live ops; repo stub + checklist shipped), **continuous compliance attestation** **100** (**todo** live SIEM/GRC; stub shipped), **external audit portal / reviewer SSO** **101** (**todo** live IdP; stub shipped), **cross-border regulatory evidence exchange** **102** (**todo** live ops; overflow stub shipped), **industry vertical regulator programs** **103** (**todo** live; overflow stub shipped), **sovereign data residency / localization** **104** (**todo** live; overflow stub shipped), **regulatory filing calendars** **105** (**todo** live; overflow stub shipped), **continuous control monitoring / regulator examinations** **106** (**todo** live; overflow stub shipped), **supervisory remediation / enforcement** **107** (**todo** live programs; overflow stub shipped), **resolution planning / consent decrees / crisis escalation** **108** (**todo** live theater; overflow stub shipped), **cross-border insolvency restructuring forum** **109** (**todo** live forums; overflow stub shipped), **creditor hierarchy / deposit-insurance bridge** **110** (**todo** live; overflow stub shipped), **resolution funding / liquidity backstop** **111** (**todo** live; overflow stub shipped), **systemic risk capital buffer / recovery-planning** **112** (**todo** live; overflow stub shipped), **digital operational resilience / critical third-party dependency** **113** (**todo** live; overflow stub shipped), **nested outsourcing / fourth-party concentration** **114** (**todo** live; overflow stub shipped), **supply-chain cascade / substitutability matrix** **115** (**todo** live; overflow stub shipped), **geopolitical supply disruption / sanctions escalation** **116** (**todo** live; overflow stub shipped), **counterparty sanctions screening overflow** **117** (**todo** live; overflow stub shipped), **export control dual-use commodity routing overflow** **118** (**todo** live sealed attestations; overflow stub + **`GET`** shipped), **end-user certificate / destination control overflow** **119** (**todo** live sealed bodies; overflow stub + **`GET`** shipped), **trade finance documentary compliance** **120** (**todo** live sealed filings; overflow stub + **`GET`** shipped), **cross-border correspondent banking / payment-rail** **121** (**todo** live; overflow stub + **`GET`** shipped), **instant payment / domestic RTGS liquidity bridge** **122** (**todo** live; overflow stub + **`GET`** shipped), **tokenized deposit / stablecoin / wholesale MM** **123** (**todo** live; overflow stub + **`GET`** shipped), **programmable money / DVP / CCP-style settlement rail** **124** (**todo** live; overflow stub + **`GET`** shipped), **wholesale CBDC bridge / interop** **125** (**todo** live bridge programs; overflow stub + **`GET` shipped**), **126** world-living tick-runtime contracts (**done**), **127** region tick lease shard authority contracts (**done**; `shard-authority-v1/` + lease fixture), **128** tick host stub batch commit (**done**; `tick-host-stub-v1/` + fixture), **129** tick production posture lease row (**done**; `tick-production-posture-v1/` + fixture), **130** world tick cross-fixture CI harness (**done**; `validate-world-tick-host-cross-fixtures.ps1` + `tick-host-executable-stub-v1/`), **131** fleet deployment posture stub (**done**; `tick-host-fleet-stub-v1/` + `tick-host-fleet-deployment-posture-stub-v1.json`), **132** live tick host cluster integration (**todo**), **133** battle encounter directive golden fixture (**done**; `systems/battle-npc-ai/fixtures/` + `check-battle-npc-ai-contracts-policy.ps1`), **134** battle arc + live-ops stub + cross-fixtures (**done**; `validate-battle-npc-ai-cross-fixtures.ps1`), **135** battle directive runtime posture stub (**done**), **136** battle directive live executor stores audit (**todo**), **137** dungeon run authority posture stub (**done**), **138** craft station authority posture stub (**done**), **139** dungeon live run authority executor (**todo**), **140** craft station live authority executor (**todo**), **141** summon continuity authority posture stub (**done**), **142** summon continuity live authority executor (**todo**), **143** combat resolution authority posture stub (**done**), **144** combat resolution live authority executor (**todo**), **145** fluids band authority posture stub (**done**), **146** fluids band live authority executor (**todo**), **147** presentation scale authority posture stub (**done**), **148** presentation scale live authority executor (**todo**), etc.).  
- Policy scripts: many checks under `scripts/check-*-policy.ps1`; backlog helper `scripts/backlog-policy-task-presence.ps1` supplies `Test-BacklogTaskSignal` for `TASK-USER-*` vs `TASK-FEAT-*` alignment.

## Automation / freshness

- `automation/automation-config.json` → `bots.testFreshnessPolicy.maxAgeHours` (currently **240**) avoids false reds when `reports/bots/` integration JSON is not regenerated every day. **Tighten toward ~24** once CI regenerates those reports on each merge (documented in design-gaps catalog).  
- `bots.dynamicSubbotOrchestration.simulatedWorkMsPerSubbot` (default **400** in config) sizes the parallel-efficiency benchmark so job startup noise does not flake against `requiredSpeedupRatio`.  
- `scripts/run-backlog-autopilot.ps1` maps in-progress tasks to policy scripts when `checkScript` is null (see `Resolve-TaskCheckScript`).

## Engine layout (recent verticals)

| Area | Path |
|------|------|
| Living world sim | `engine/sim/world-living-v1/` |
| Biomes / weather stub | `engine/sim/world-living-v1/biome-weather-v1/` |
| Spawn conditions stub | `engine/sim/world-living-v1/spawn-conditions-v1/` |
| World-living JSON fixtures | `systems/world-living/fixtures/` (**`126`**–**`129`** fixtures **done**; golden corpus cross-check **`130`** **done** via `validate-world-tick-host-cross-fixtures.ps1`; fleet posture **`131`** **done** via `tick-host-fleet-deployment-posture-stub-v1.json`; live host **`132`** **todo**) |
| World-living tick runtime (contracts + CI + fleet posture stub) | `engine/sim/world-living-v1/tick-runtime-v1/` (`shard-authority-v1/`, `tick-host-stub-v1/`, `tick-production-posture-v1/`, `tick-host-executable-stub-v1/`, `tick-host-fleet-stub-v1/`) |
| Presentation / multiverse | `engine/render/presentation-multiverse-v1/` |
| Vulkan baseline | `engine/render/scalability-vulkan-v1/` |
| Mega-scale / quality | `engine/render/mega-scale-quality-v1/` |
| Render vertical index | `engine/render/README.md` |
| Presentation scale authority posture stub | `engine/render/presentation-scale-authority-posture-stub-v1/` (**`147`** **done**; live executor **`148`** **todo**) |
| Presentation scale systems fixtures | `systems/presentation-scale/fixtures/` (**`147`** posture stub **done**; **`148`** **todo**) |
| Fluids / naval / atmosphere | `engine/sim/fluids-atmosphere-v1/` (`hydro-budget-v1`, `naval-ages-v1`, `atmosphere-column-v1`, `fluids-band-posture-stub-v1`) |
| Fluids / atmosphere systems fixtures | `systems/fluids-atmosphere/fixtures/` (**`145`** band authority posture stub **done**; live executor **`146`** **todo**) |
| Battle / named NPC / live-ops hooks | `engine/sim/battle-npc-ai-v1/` (`encounter-ai-v1`, `named-npc-lifecycle-v1`, `liveops-npc-hooks-v1`, `directive-runtime-posture-stub-v1`) |
| Battle / named NPC AI JSON fixtures | `systems/battle-npc-ai/fixtures/` (**`133`** encounter envelope **done**; **`134`** arc + live-ops stub + cross-validator **done**; posture **`135`** **done**; live executor **`136`** **todo**) |
| Combat style / war UX / perspectives / RTS | `engine/combat/combat-style-v1/` (`identity-styles-v1`, `melee-skill-v1`, `war-ux-progression-v1`, `perspective-v1`, `rts-unlock-v1`, `combat-resolution-posture-stub-v1`) |
| Combat style systems fixtures | `systems/combat-style/fixtures/` (**`143`** resolution authority posture stub **done**; live executor **`144`** **todo**) |
| Summons / mortality / recovery | `engine/sim/summon-mortality-v1/` (`summon-graph-v1`, `resurrection-continuity-v1`, `reincarnation-v1`, `death-recovery-magic-v1`, `summon-continuity-posture-stub-v1`) |
| Summon / mortality systems fixtures | `systems/summon-mortality/fixtures/` (**`141`** continuity authority posture stub **done**; live executor **`142`** **todo**) |
| Dungeon autonomy / persistence | `engine/sim/dungeon-autonomy-v1/` (`dungeon-master-v1`, `persistence-collapse-v1`, `dungeon-run-posture-stub-v1`) |
| Dungeon autonomy systems fixtures | `systems/dungeon-autonomy/fixtures/` (**`137`** run authority posture stub **done**; live executor **`139`** **todo**) |
| Craft / forge / mastery | `engine/sim/craft-forge-v1/` (`physical-crafting-v1`, `repair-specialist-v1`, `enchant-mastery-v1`, `craft-station-posture-stub-v1`) |
| Craft / forge systems fixtures | `systems/craft-forge/fixtures/` (**`138`** station authority posture stub **done**; live executor **`140`** **todo**) |
| Appendix batches & tone catalog | `engine/design/appendix-catalog-v1/` (`batch-a-v1`, `batch-b-v1`, `batch-c-v1`, `overlord-catalog-v1`) |
| Tooling / observability harnesses | `engine/ops/tooling-harness-v1/` (`regional-economy-stress-v1`, `spell-ergonomics-heatmap-v1`, `summon-control-advisor-v1`, `boss-enhancement-telemetry-v1`, `legendary-fx-budget-v1`, `social-bias-fuzzer-v1`, `equipment-lifecycle-audit-v1`, `cast-readiness-overlay-v1`, `regression-spotlight-v1`, `faction-conflict-forecast-v1`, `condition-crafting-recommendations-v1`, `authority-reason-explorer-v1`) |
| Unified tooling cockpit shell | `engine/ops/unified-tooling-cockpit-v1/` (`shell-contract-v1`, `harness-card-registry-v1/cockpit-harness-index-v1.json`, `continuity-adapters-v1/` **`084`**/**`085`**/**`087`** manifest /**`089`** gateway contract /**`090`** deployment stub /**`091`** container pack /**`092`** go-live /**`093`** multiplex /**`094`** operational soak /**`095`** hosted integration /**`096`** tenant operations /**`097`** live delivery /**`098`** tenant-bound /**`099`** multiregion overflow /**`100`** compliance attestation /**`101`** external audit portal overflow /**`102`** cross-border overflow /**`103`** industry vertical overflow /**`104`** sovereign localization overflow /**`105`** regulatory filing calendar overflow /**`106`** CCM / regulator examination overflow /**`107`** supervisory remediation enforcement overflow /**`108`** resolution planning crisis supervisory overflow /**`109`** cross-border insolvency restructuring forum overflow /**`110`** creditor hierarchy deposit-insurance bridge overflow /**`111`** resolution funding liquidity backstop overflow /**`112`** systemic risk capital buffer recovery planning overflow /**`113`** digital operational resilience critical third-party overflow /**`114`** nested outsourcing fourth-party concentration overflow /**`115`** supply-chain cascade substitutability matrix overflow /**`116`** geopolitical supply disruption sanctions escalation overflow /**`117`** counterparty sanctions screening program overflow /**`118`** export control dual-use commodity routing overflow /**`119`** end-user certificate destination control program overflow /**`120`** trade finance documentary compliance overflow /**`121`** cross-border correspondent banking payment-rail overflow /**`122`** instant payment RTGS liquidity-bridge overflow /**`123`** tokenized deposit stablecoin wholesale MM /**`124`** programmable money DVP settlement rail /**`125`** wholesale CBDC bridge interop overflow stub) |
| Continuity gateway pack (Dockerfile / Helm / optional ingress / optional HPA) | `engine/ops/continuity-gateway-pack-v1/` (**`091`**; ingress **`093`**; HPA **`094`**) |
| Continuity gateway CI (workflow) | `.github/workflows/continuity-gateway-pack.yml` (**`092`** + **`helm template`** ingress/HPA smoke **`094`**) |
| Observability vendor posture | `engine/ops/observability-vendor-posture-v1/` |
| Observability vendor integration (manifest stub) | `engine/ops/observability-vendor-integration-v1/` (`adapter-manifest-stub-v1.json`, `rollout-checklist-v1.md`) |
| Enterprise observability operations | `engine/ops/enterprise-observability-ops-v1/` (`escalation-matrix-stub-v1.json`, `oncall-rotation-stub-v1.json`, `regional-failover-drill-checklist-v1.md`) |
| Precursor canon / incident feed | `engine/sim/world-history-v1/` (`great-conflict-timeline-v1`, `third-reset-marker-v1`, `evidence-ledger-v1`, `narrative-incident-feed-v1`, `archival-canon-posture-v1`, `archival-production-pilot-v1`, `archival-hyperscale-posture-v1`) + fixtures `systems/world-history/fixtures/` |
| Live paging / MSSP (repo stubs only) | `engine/ops/live-paging-mssp-contract-v1/` |
| Ops vertical entrypoint | `engine/ops/README.md` |
| Continuity lane index (tooling → observability → paging / archival ordering) | `engine/ops/continuity-ops-lane-v1/` |
| Local operator dashboard (incl. continuity APIs) | `dashboard/index.html` + `scripts/dashboard-server.ps1` (`GET /api/continuity-cockpit` **`taskFeatRef` `125`** aggregates vertical chain + gateway stubs through **`125`**; `GET /api/continuity-stream-manifest` **`087`**; `GET /api/continuity-production-gateway-contract` **`089`**; `GET /api/continuity-gateway-service-deployment` **`090`**; `GET /api/continuity-gateway-container-pack` **`091`**; `GET /api/continuity-gateway-golive-posture` **`092`**; `GET /api/continuity-gateway-cluster-multiplex-posture` **`093`**; `GET /api/continuity-gateway-operational-soak` **`094`**; `GET /api/continuity-gateway-hosted-integration` **`095`**; `GET /api/continuity-gateway-tenant-operations` **`096`**; `GET /api/continuity-gateway-live-delivery` **`097`**; `GET /api/continuity-gateway-tenant-bound` **`098`**; `GET /api/continuity-gateway-multiregion-mesh-overflow` **`099`**; `GET /api/continuity-gateway-compliance-attestation` **`100`**; `GET /api/continuity-gateway-external-audit-portal-overflow` **`101`**; `GET /api/continuity-gateway-cross-border-regulatory-evidence-overflow` **`102`**; `GET /api/continuity-gateway-industry-vertical-regulator-overflow` **`103`**; `GET /api/continuity-gateway-sovereign-data-localization-overflow` **`104`**; `GET /api/continuity-gateway-regulatory-filing-calendar-overflow` **`105`**; `GET /api/continuity-gateway-ccm-regulator-examination-overflow` **`106`**; `GET /api/continuity-gateway-supervisory-remediation-enforcement-overflow` **`107`**; `GET /api/continuity-gateway-resolution-planning-crisis-supervisory-overflow` **`108`**; `GET /api/continuity-gateway-cross-border-insolvency-restructuring-forum-overflow` **`109`**; `GET /api/continuity-gateway-creditor-hierarchy-deposit-insurance-bridge-overflow` **`110`**; `GET /api/continuity-gateway-resolution-funding-liquidity-backstop-overflow` **`111`**; `GET /api/continuity-gateway-systemic-risk-capital-buffer-recovery-planning-overflow` **`112`**; `GET /api/continuity-gateway-digital-operational-resilience-critical-third-party-overflow` **`113`**; `GET /api/continuity-gateway-nested-outsourcing-fourth-party-concentration-overflow` **`114`**; `GET /api/continuity-gateway-supply-chain-cascade-substitutability-matrix-overflow` **`115`**; `GET /api/continuity-gateway-geopolitical-supply-disruption-sanctions-escalation-overflow` **`116`**; `GET /api/continuity-gateway-counterparty-sanctions-screening-program-overflow` **`117`**; `GET /api/continuity-gateway-export-control-dual-use-commodity-routing-overflow` **`118`**; `GET /api/continuity-gateway-end-user-certificate-destination-control-program-overflow` **`119`**; `GET /api/continuity-gateway-trade-finance-documentary-compliance-overflow` **`120`**; `GET /api/continuity-gateway-cross-border-correspondent-banking-payment-rail-overflow` **`121`**; `GET /api/continuity-gateway-instant-payment-and-rtgs-liquidity-bridge-overflow` **`122`**; `GET /api/continuity-gateway-tokenized-deposit-stablecoin-wholesale-money-market-overflow` **`123`**; `GET /api/continuity-gateway-programmable-money-dvp-settlement-rail-overflow` **`124`**; `GET /api/continuity-gateway-wholesale-cbdc-bridge-interop-overflow` **`125`**; optional dev SSE **`088`** `GET /api/continuity-soc-events` when **`CURSE_CONTINUITY_SSE_ENABLE=1`**) |

Implementation plans live under `implementation-plans/` and should be referenced from the matching **`implementationPlanPath`** field on FEAT rows.

## Precursor / ledger follow-up split

- **`TASK-FEAT-061`** — thirteen great conflicts chronology (**done**; fixture `great-conflicts-v1.json`).  
- **`TASK-FEAT-062`** — third global reset stone-age marker (**done**; fixture `third-global-reset-stone-age-v1.json`).  
- **`TASK-FEAT-063`** — precursor influence evidence ledger slice (**done**; fixture `evidence-ledger-record-shape-v1.json`).  
- **`TASK-FEAT-006`** — narrative incident timeline integration (**done**; consumes **061–063**).  
- Policy guard: `scripts/check-precursor-reset-cycle-lore-policy.ps1`.

## Deferred / overflow FEAT rows (beyond slice union)

- **`TASK-FEAT-064`** — advanced GPU (mesh shaders, RT, GI) after presentation baseline.  
- **`TASK-FEAT-065`** — full CFD-grade hydrodynamics and player-scale interactive waves (deferred past V1 fluids vertical).  
- **`TASK-FEAT-066`** — learned/ML battle policies and full procedural personality for all NPCs (deferred past V1 battle-NPC vertical).  
- **`TASK-FEAT-067`** — full AAA RTS per-unit fidelity and motion-matched melee animation depth (deferred past V1 combat-style vertical).  
- **`TASK-FEAT-068`** — cross-shard persistent summon soulbind / unbounded duplication exploration (deferred past V1 summon-mortality vertical).  
- **`TASK-FEAT-069`** — LLM or heavy procedural narrative dungeon master and unbounded procedural scaling (deferred past V1 dungeon vertical).  
- **`TASK-FEAT-070`** — ultra-fidelity physical crafting (molecular / soft-body / VR-haptics parity) deferred past V1 craft-forge vertical.  
- **`TASK-FEAT-071`** — full appendix mechanics runtime, scenario suites, and production implementation deferred past V1 design-batch / catalog vertical.  
- **`TASK-FEAT-072`** — unified live-ops / CI tooling **cockpit shell** (card registry, fail-open adapters; **`049`–`060`** referenced): **done** (`engine/ops/unified-tooling-cockpit-v1/`).  
- **`TASK-FEAT-073`** — vendor posture pilot matrix residency fairness rollback (**done**; `engine/ops/observability-vendor-posture-v1/`).  
- **`TASK-FEAT-074`** — archival/cinematic/ML **governance posture** (**done**; `archival-canon-posture-v1/`).  
- **`TASK-FEAT-075`** — realtime **operator plane** contract (optional push, RBAC posture doc; **done**).  
- **`TASK-FEAT-076`** — vendor **integration manifest** (adapter stub, rollout checklist; **done**; `observability-vendor-integration-v1/`).  
- **`TASK-FEAT-077`** — archival **production pilot** stub contracts (index, cinema job, ML provenance; **done**; `archival-production-pilot-v1/`).  
- **`TASK-FEAT-078`** — enterprise **ops** contracts (escalation, on-call, failover drills; **done**; `enterprise-observability-ops-v1/`).  
- **`TASK-FEAT-079`** — archival **hyperscale posture** stubs: index topology, render pool, ML hosting boundary (**done**; `archival-hyperscale-posture-v1/`).  
- **`TASK-FEAT-080`** — live **paging route** and **MSSP handoff envelope** contract stubs (**done**; `live-paging-mssp-contract-v1/`).  
- **`TASK-FEAT-081`** — archival hyperscale **go-live** (multiregion customer UX, hosted ML execution) after **`079`** posture — operational, mostly outside repo.  
- **`TASK-FEAT-082`** — live paging / MSSP / compliance **go-live** after **`080`** stubs — legal staffing and secrets outside repo.  
- **`TASK-FEAT-083`** — continuity ops lane **index** (README + chain stub; **`engine/ops/README.md`** entrypoint): **done**.  
- **`TASK-FEAT-084`** — cockpit **continuity slot registry** (contract stubs, doc pointers only; **`continuity-adapters-v1/`**): **done**.  
- **`TASK-FEAT-085`** — cockpit continuity **runtime binding** stub (`continuity-runtime-binding-stub-v1.json`; repo-relative bot-report globs): **done**.  
- **`TASK-FEAT-086`** — cockpit continuity **dashboard read-only** panel (`GET /api/continuity-cockpit`, `dashboard/index.html` card): **done**.  
- **`TASK-FEAT-087`** — cockpit continuity **live stream manifest** (env-var names, channel kinds, security posture; **`continuity-live-stream-manifest-stub-v1.json`**): **done**.  
- **`TASK-FEAT-088`** — continuity **dev SSE gateway** (`GET /api/continuity-soc-events`, env **`CURSE_CONTINUITY_SSE_ENABLE`**): **done**.  
- **`TASK-FEAT-089`** — production **gateway contract** stub (**`continuity-production-wss-gateway-contract-stub-v1.json`**; **done**).  
- **`TASK-FEAT-090`** — gateway **deployment posture stub** (**`continuity-gateway-service-deployment-stub-v1.json`**; **done**).  
- **`TASK-FEAT-091`** — gateway **container pack** (Dockerfile, Helm scaffold, stub Node gateway; **`engine/ops/continuity-gateway-pack-v1/`**): **done**.  
- **`TASK-FEAT-092`** — gateway **CI workflow** (`docker build`, `helm lint`) + **go-live posture** stub (**`continuity-gateway-golive-posture-stub-v1.json`**): **done**.  
- **`TASK-FEAT-093`** — gateway **cluster multiplex posture** stub (JSON, rollout checklist, optional Helm ingress, dashboard API): **done**.  
- **`TASK-FEAT-094`** — **operational soak** stub (registry env **names**, soak docs, optional **HPA**, **`server.mjs`** metrics + multiplex echo, CI **`helm template`** smoke, dashboard API): **done**.  
- **`TASK-FEAT-095`** — **hosted integration posture** stub (CI registry docs, optional **`workflow_dispatch`** GHCR publish via **`GITHUB_TOKEN`**, MSSP drill checklist pointers, terraform README layout, **`continuity-gateway-hosted-integration-posture-stub-v1.json`**, dashboard API): **done**.  
- **`TASK-FEAT-096`** — **tenant operations posture** stub (OIDC/registry pattern docs, remote-state hints, MSSP evidence metadata envelope, **`terraform/modules/`** README, **`continuity-gateway-tenant-operations-posture-stub-v1.json`**, dashboard API): **done**.  
- **`TASK-FEAT-097`** — **live delivery posture** stub (rollout checklist, CI integration-test contract env names, MSSP vault handoff env names, **`continuity-gateway-live-delivery-posture-stub-v1.json`**, **`validate-continuity-gateway-json-smoke.ps1`**, dashboard API): **done**.  
- **`TASK-FEAT-098`** — **tenant-bound** posture + CI (**stub JSON**, **`terraform/stub/ci-smoke`**, **`GET /api/continuity-gateway-tenant-bound`** shipped); **live** cloud apply, OIDC registry automation, integration tests, MSSP vault uploads — paired with **`082`** (**todo** until tenant credentials wired).  
- **`TASK-FEAT-099`** — **multi-region mesh**, MSSP **SOAR**, **hosted SOC** backbone — operational/commercial overflow (**todo** live ops; **repo stub + checklist + APIs shipped**).  
- **`TASK-FEAT-100`** — **continuous compliance attestation** pipelines (SIEM/GRC correlation, signed exports) — tenant/vendor automation (**todo** live; **stub + checklist + API shipped**).  
- **`TASK-FEAT-101`** — **external auditor portal**, trust-center workflows, **reviewer SSO** federation — vendor/legal overflow (**todo** live; **stub + checklist + API shipped**).  
- **`TASK-FEAT-102`** — **cross-border regulatory evidence exchange** (MOUs, clearinghouses, residency routing) — diplomatic/legal overflow (**todo** live; **overflow stub + checklist + API shipped**).  
- **`TASK-FEAT-103`** — **industry vertical regulator programs** (finance, health, games, transport) and **supranational API federation** — vendor/legal overflow (**todo** live; **overflow stub + checklist + API shipped**).  
- **`TASK-FEAT-104`** — **sovereign data residency**, **localization**, and **lawful-access routing** attestation — cloud/counsel overflow (**todo** live; **overflow stub + checklist + API shipped**).  
- **`TASK-FEAT-105`** — **regulatory filing calendars** and **scheduled portal submissions** — GRC operations overflow (**todo** live; **overflow stub + checklist + API shipped**).  
- **`TASK-FEAT-106`** — **continuous control monitoring** evidence loops and **regulator examination** coordination — counsel/GRC overflow (**todo** live; **overflow stub + checklist + API shipped**).  
- **`TASK-FEAT-107`** — **supervisory remediation**, **MRAs/MRIAs**, **consent orders**, and **regulatory enforcement** program coordination — legal/GRC overflow (**todo** live programs; overflow stub **shipped**).  
- **`TASK-FEAT-108`** — **resolution planning**, **consent decree coordination**, and **crisis supervisory escalation** — board/counsel overflow (**todo** live crisis theater; overflow stub **shipped**).  
- **`TASK-FEAT-109`** — **cross-border insolvency restructuring forums** — tribunal/counsel overflow (**todo** live forums; overflow stub **shipped**).  
- **`TASK-FEAT-110`** — **creditor hierarchy** and **deposit-insurance bridge** coordination — resolution-authority overflow (**todo** live; repo stub + checklist + **`GET`** shipped).
- **`TASK-FEAT-111`** — **resolution funding** and **liquidity backstop facility** coordination — treasury overflow (**todo** live; repo stub + checklist + **`GET`** shipped).
- **`TASK-FEAT-112`** — **systemic risk capital buffer** and **recovery-planning attestation** coordination — macro-prudential overflow (**todo** live; repo stub + checklist + **`GET`** shipped).
- **`TASK-FEAT-113`** — **digital operational resilience** and **critical third-party dependency** governance — ICT/vendor procurement overflow (**todo** live; repo stub + checklist + **`GET`** shipped).
- **`TASK-FEAT-114`** — **nested outsourcing** and **fourth-party concentration** governance — extended vendor-risk overflow (**todo** live; overflow stub **shipped**).
- **`TASK-FEAT-115`** — **supply-chain cascade** and **substitutability matrix** governance — vendor-risk / GRC overflow (**todo** live; overflow stub **shipped**).
- **`TASK-FEAT-116`** — **geopolitical supply disruption** and **sanctions escalation** governance — trade-compliance overflow (**todo** live; overflow stub **shipped**).
- **`TASK-FEAT-117`** — **counterparty sanctions screening program** governance — vendor-risk / trade-compliance overflow (**todo** live; overflow stub **shipped**).
- **`TASK-FEAT-118`** — **export control dual-use commodity routing** governance — overflow stub + checklist + **`GET`** **shipped**; sealed attestations / matrices remain **`118` todo**; deferral key **`exportControlDualUseCommodityRoutingAttestationDeferred`** in **`continuity-vertical-chain-stub-v1.json`** for live programs.  
- **`TASK-FEAT-119`** — **end-user certificate / destination control overflow** governance — overflow stub + checklist + **`GET`** **shipped**; sealed certificate / EUC bodies remain **`119` todo**; deferral key **`endUserCertificateDestinationControlProgramDeferred`** for sealed programs; overflow pointer **`tradeFinanceDocumentaryComplianceOverflowStub`** / **`tradeFinanceDocumentaryComplianceAttestationDeferred`** → **`120`**.  
- **`TASK-FEAT-120`** — **trade finance documentary compliance** — overflow stub + checklist + **`GET`** **shipped**; live LC / documentary / bank sealed filings remain **`120` todo**; deferral key **`tradeFinanceDocumentaryComplianceAttestationDeferred`**; tail **`crossBorderCorrespondentBankingAndPaymentRailAttestationDeferred`** → **`121`**.  
- **`TASK-FEAT-121`** — **cross-border correspondent banking and payment-rail** — overflow stub + checklist + **`GET`** **shipped**; **live** correspondent attestations remain **`121` todo**; **`instantPaymentAndRtgsLiquidityBridgeAttestationDeferred`** → **`122`**.  
- **`TASK-FEAT-122`** — **instant payment / domestic RTGS liquidity bridge** — overflow stub + checklist + **`GET`** **shipped**; **live** instant-rail bodies remain **`122` todo**; **`tokenizedDepositStablecoinAndWholesaleMoneyMarketAttestationDeferred`** → **`123`**.  
- **`TASK-FEAT-123`** — **tokenized deposit / stablecoin / wholesale money-market** — overflow stub + checklist + **`GET`** **shipped**; **live** sealed token/MM bodies remain **`123` todo**; **`programmableMoneyAndDvpSettlementRailAttestationDeferred`** → **`124`**.  
- **`TASK-FEAT-124`** — **programmable money / DVP / CCP-style settlement rail** — overflow stub + checklist + **`GET`** **shipped**; **live** DVP / netting bodies remain **`124` todo**; **`cbdcBridgeAndInteropAttestationDeferred`** → **`125`**.  
- **`TASK-FEAT-125`** — **wholesale CBDC bridge / cross-venue interoperability** — overflow stub + checklist + **`GET /api/continuity-gateway-wholesale-cbdc-bridge-interop-overflow`** **shipped**; **live** bridge proofs / interop matrices remain **`125` todo**; **`cbdcBridgeAndInteropAttestationDeferred`** stays for live programs.  
- **`TASK-FEAT-126`** — **tick-runtime contracts, ecology→spawn sequencing, replay header fixtures** — **done** (`tick-runtime-v1/`, `systems/world-living/fixtures/`, strict policy).  
- **`TASK-FEAT-127`** — **region tick lease grant shard authority contracts** — **done** (`shard-authority-v1/`, `region-tick-lease-grant-v1.json`; **`068`** overlap noted in README).  
- **`TASK-FEAT-128`** — **tick host stub README + `tick_host.batch_commit.v1` fixture + strict policy** — **done** (`tick-host-stub-v1/`).  
- **`TASK-FEAT-129`** — **tick production posture** (lease table row fixture, RPC operation names, wall-clock env hints) — **done** (`tick-production-posture-v1/`).  
- **`TASK-FEAT-130`** — **cross-fixture CI harness** (`validate-world-tick-host-cross-fixtures.ps1`, `tick-host-executable-stub-v1/`) — **done**.  
- **`TASK-FEAT-131`** — **fleet deployment posture stub** (`tick-host-fleet-stub-v1/`, `tick-host-fleet-deployment-posture-stub-v1.json`, strict policy) — **done**.  
- **`TASK-FEAT-132`** — **live tick host cluster integration** (runnable image, lease DSN, RPC transports, Helm apply) — **todo** (overflow past **`131`**).  
- **`TASK-FEAT-133`** — **battle encounter directive golden fixture** + systems index + **`check-battle-npc-ai-contracts-policy.ps1` Strict** — **done** (`systems/battle-npc-ai/`).  
- **`TASK-FEAT-134`** — **named NPC arc + live-ops knob stubs** + **`validate-battle-npc-ai-cross-fixtures.ps1`** — **done**.  
- **`TASK-FEAT-135`** — **directive runtime posture stub** (fixture + README + strict policy + cross pin) — **done** (overflow narrowed from **`134`**).  
- **`TASK-FEAT-136`** — **in-process directive / arc runtime wiring** (executor, live stores, audit sink) — **todo** (overflow past **`135`**).  
- **`TASK-FEAT-137`** — **dungeon run authority posture stub** (systems fixture + README + strict policy) — **done** (`systems/dungeon-autonomy/`, `implementation-plans/TASK-FEAT-137-DUNGEON-RUN-AUTHORITY-POSTURE-STUB-V1.md`).  
- **`TASK-FEAT-138`** — **craft station authority posture stub** (systems fixture + README + strict policy) — **done** (`systems/craft-forge/`, `implementation-plans/TASK-FEAT-138-CRAFT-STATION-AUTHORITY-POSTURE-STUB-V1.md`).  
- **`TASK-FEAT-139`** — **dungeon live run authority executor** (shard, collapse stores, replay export) — **todo** (`implementation-plans/TASK-FEAT-139-DUNGEON-RUN-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`; distinct from **`069`** LLM DM).  
- **`TASK-FEAT-140`** — **craft station live authority executor** (shard, recipe graph store, replay export) — **todo** (`implementation-plans/TASK-FEAT-140-CRAFT-STATION-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`; distinct from **`070`** ultra-fidelity).  
- **`TASK-FEAT-141`** — **summon continuity authority posture stub** (systems fixture + README + strict policy) — **done** (`systems/summon-mortality/`, `implementation-plans/TASK-FEAT-141-SUMMON-CONTINUITY-AUTHORITY-POSTURE-STUB-V1.md`).  
- **`TASK-FEAT-142`** — **summon continuity live authority executor** (shard, graph edge store, replay export) — **todo** (`implementation-plans/TASK-FEAT-142-SUMMON-CONTINUITY-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`; distinct from **`068`** cross-shard soulbind).  
- **`TASK-FEAT-143`** — **combat resolution authority posture stub** (systems fixture + README + strict policy) — **done** (`systems/combat-style/`, `implementation-plans/TASK-FEAT-143-COMBAT-RESOLUTION-AUTHORITY-POSTURE-STUB-V1.md`).  
- **`TASK-FEAT-144`** — **combat resolution live authority executor** (shard, style-ledger timeline store, replay export) — **todo** (`implementation-plans/TASK-FEAT-144-COMBAT-RESOLUTION-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`; distinct from **`067`** AAA RTS motion).  
- **`TASK-FEAT-145`** — **fluids band authority posture stub** (systems fixture + README + strict policy) — **done** (`systems/fluids-atmosphere/`, `implementation-plans/TASK-FEAT-145-FLUIDS-BAND-AUTHORITY-POSTURE-STUB-V1.md`).  
- **`TASK-FEAT-146`** — **fluids band live authority executor** (shard, hydro transition ledger, replay export) — **todo** (`implementation-plans/TASK-FEAT-146-FLUIDS-BAND-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`; distinct from **`065`** full CFD).  
- **`TASK-FEAT-147`** — **presentation scale authority posture stub** (systems fixture + `engine/render/README.md` + strict policy) — **done** (`systems/presentation-scale/`, `implementation-plans/TASK-FEAT-147-PRESENTATION-SCALE-AUTHORITY-POSTURE-STUB-V1.md`).  
- **`TASK-FEAT-148`** — **presentation scale live authority executor** (shard, cohort envelope ledger, swapchain coordination, replay export) — **todo** (`implementation-plans/TASK-FEAT-148-PRESENTATION-SCALE-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`; distinct from **`064`** advanced GPU).  

## Standing follow-ups (process)

- Drive **`TASK-FEAT-082`** for production paging and MSSP execution (`080` stubs shipped; `078` ops matrix); **`TASK-FEAT-081`** when funding hyperscale archival UX (`079` posture; `077` pilot; `074` governance); live vendor URLs tokens and auditor PDFs remain outside git (`073`/`076` contracts).  
- Use **`TASK-FEAT-098`** for **live** tenant automation (**Terraform apply**, registry OIDC in CI, MSSP vault evidence); **`TASK-FEAT-099`** for **live multi-region / SOAR / hosted SOC** overflow (stub/json already in repo); **`TASK-FEAT-100`** for **live compliance attestation** automation beyond **`082`** drill custody (stub/json already in repo); **`TASK-FEAT-101`** for **live auditor portals and SSO** (stub/json already in repo); **`TASK-FEAT-102`** for **live cross-border regulator programs** outside git; **`TASK-FEAT-103`** for **sector regulator enrollment and federated APIs** outside git; **`TASK-FEAT-104`** for **sovereign residency and lawful routing** outside git; **`TASK-FEAT-105`** for **filing calendars and regulator portals** outside git; **`TASK-FEAT-106`** for **continuous control monitoring and regulator examinations** outside git; **`TASK-FEAT-107`** for **supervisory remediation and regulatory enforcement programs** outside git; **`TASK-FEAT-108`** for **live crisis resolution / consent decree theater** outside git; **`TASK-FEAT-109`** for **live cross-border insolvency forums** outside git; **`TASK-FEAT-110`** for **creditor hierarchy / deposit insurance bridge** execution outside git; **`TASK-FEAT-111`** for **resolution funding / liquidity backstop** execution outside git; **`TASK-FEAT-112`** for **systemic risk buffers / recovery-plan attestations** outside git; **`TASK-FEAT-113`** for **ICT operational resilience / critical third-party dependency** programs outside git; **`TASK-FEAT-114`** for **nested outsourcing / fourth-party concentration** analytics outside git; **`TASK-FEAT-115`** for **supply-chain cascade / substitutability matrix** tooling outside git; **`TASK-FEAT-116`** for **geopolitical supply disruption / sanctions escalation** programs outside git; **`TASK-FEAT-117`** for **counterparty sanctions screening** live engines outside git; **`TASK-FEAT-118`** for **export control dual-use sealed attestations / matrices** outside git (overflow stub shipped); **`TASK-FEAT-119`** for **end-user sealed certificates / destination control bodies** outside git (overflow stub shipped); **`TASK-FEAT-120`** for **trade finance documentary / LC / bank trade sealed filings** outside git (overflow stub shipped); **`TASK-FEAT-121`** for **correspondent banking / payment-rail / RMA-style attestations** outside git (overflow stub shipped); **`TASK-FEAT-122`** for **instant payment / RTGS liquidity-bridge sealed bodies** outside git (overflow stub shipped); **`TASK-FEAT-123`** for **tokenized deposits / stablecoin reserves / wholesale MM sealed bodies** outside git (overflow stub shipped); **`TASK-FEAT-124`** for **programmable money / DVP / CCP-style sealed rail bodies** outside git (overflow stub shipped); **`TASK-FEAT-125`** for **wholesale CBDC bridge / cross-venue interoperability live attestations** outside git (repo stub + **`GET` shipped**); **`097`** ships **live delivery posture** (checklists, env-var contracts, JSON smoke); **`096`** ships **tenant operations posture JSON + doc pointers only** (no cloud identities in git); **`095`** ships **hosted integration posture + CI docs + optional GHCR**; **`094`** ships **operational soak JSON + metrics + optional HPA + soak docs**; **`093`** ships **multiplex posture + optional ingress**; **`092`** ships **CI validation + go-live posture JSON**; **`091`** is the **buildable pack**; **`090`** is deployment stub JSON; **`089`** is gateway contract; **`088`** is local SSE smoke; **`087`** defines stream hooks. Keep **`086`**/**`087`**/**`089`**/**`090`**/**`091`**/**`092`**/**`093`**/**`094`**/**`095`**/**`096`**/**`097`**/**`098`**/**`099`**/**`100`**/**`101`**/**`102`**/**`103`**/**`104`**/**`105`**/**`106`**/**`107`**/**`108`**/**`109`**/**`110`**/**`111`**/**`112`**/**`113`**/**`114`**/**`115`**/**`116`**/**`117`**/**`118`**/**`119`**/**`120`**/**`121`**/**`122`**/**`123`**/**`124`**/**`125`** GET APIs read-only unless product adds authenticated writes. Honor **`084`**/**`085`** contracts and fail-open **`072`** shell rules.  
- When CI regenerates `reports/bots/` every merge, lower **`bots.testFreshnessPolicy.maxAgeHours`** toward **~24** in `automation/automation-config.json`.  
- **`TASK-FEAT-131`** ships **fleet deployment posture** stub docs + fixture (`implementation-plans/TASK-FEAT-131-WORLD-TICK-HOST-FLEET-RUNTIME-DEFERRED-V1.md`); **`TASK-FEAT-132`** tracks **live** cluster tick host + lease RPC (`implementation-plans/TASK-FEAT-132-WORLD-TICK-HOST-LIVE-CLUSTER-INTEGRATION-DEFERRED-V1.md`).  
- **`TASK-FEAT-133`**–**`134`** ship **battle/NPC AI golden fixtures** + cross-validator (`implementation-plans/TASK-FEAT-133-BATTLE-NPC-ENCOUNTER-DIRECTIVE-FIXTURE-DEFERRED-V1.md`, `TASK-FEAT-134-BATTLE-NPC-NAMED-ARC-LIVEOPS-FIXTURE-CHAIN-DEFERRED-V1.md`); **`TASK-FEAT-135`** ships **directive runtime posture stub** (`implementation-plans/TASK-FEAT-135-BATTLE-NPC-DIRECTIVE-RUNTIME-WIRING-DEFERRED-V1.md`); **`TASK-FEAT-136`** tracks **live battle executor** (`implementation-plans/TASK-FEAT-136-BATTLE-NPC-DIRECTIVE-RUNTIME-LIVE-EXECUTOR-DEFERRED-V1.md`); **`TASK-FEAT-137`** ships **dungeon run authority posture stub** (`implementation-plans/TASK-FEAT-137-DUNGEON-RUN-AUTHORITY-POSTURE-STUB-V1.md`); **`TASK-FEAT-139`** tracks **live dungeon run executor** (`implementation-plans/TASK-FEAT-139-DUNGEON-RUN-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`); **`TASK-FEAT-138`** ships **craft station posture stub** (`implementation-plans/TASK-FEAT-138-CRAFT-STATION-AUTHORITY-POSTURE-STUB-V1.md`); **`TASK-FEAT-140`** tracks **live craft station executor** (`implementation-plans/TASK-FEAT-140-CRAFT-STATION-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`); **`TASK-FEAT-141`** ships **summon continuity posture stub** (`implementation-plans/TASK-FEAT-141-SUMMON-CONTINUITY-AUTHORITY-POSTURE-STUB-V1.md`); **`TASK-FEAT-142`** tracks **live summon continuity executor** (`implementation-plans/TASK-FEAT-142-SUMMON-CONTINUITY-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`); **`TASK-FEAT-143`** ships **combat resolution posture stub** (`implementation-plans/TASK-FEAT-143-COMBAT-RESOLUTION-AUTHORITY-POSTURE-STUB-V1.md`); **`TASK-FEAT-144`** tracks **live combat resolution executor** (`implementation-plans/TASK-FEAT-144-COMBAT-RESOLUTION-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`); **`TASK-FEAT-145`** ships **fluids band posture stub** (`implementation-plans/TASK-FEAT-145-FLUIDS-BAND-AUTHORITY-POSTURE-STUB-V1.md`); **`TASK-FEAT-146`** tracks **live fluids band executor** (`implementation-plans/TASK-FEAT-146-FLUIDS-BAND-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`); **`TASK-FEAT-147`** ships **presentation scale posture stub** (`implementation-plans/TASK-FEAT-147-PRESENTATION-SCALE-AUTHORITY-POSTURE-STUB-V1.md`); **`TASK-FEAT-148`** tracks **live presentation-scale executor** (`implementation-plans/TASK-FEAT-148-PRESENTATION-SCALE-LIVE-AUTHORITY-EXECUTOR-DEFERRED-V1.md`).  
- **`scripts/check-legacy-triage-policy.ps1`** expects five **`TASK-LEGACY-*`** rows in `tasks.json`; they are currently absent — run only after restoring those mirrors or relax the policy if triage moves exclusively to **`TASK-FEAT-*`**.

## Conventions for agents

- Prefer **small, reviewable diffs**; do not rewrite unrelated backlog rows.  
- After backlog or policy edits, run **`.\scripts\quality-gate.ps1 -Strict`** from repo root (PowerShell).  
- When adding new **`TASK-FEAT-*`** rows, respect `backlog/tasks.schema.json` id pattern and register epics in `epics.json`.


---

## Hub-Managed Workflow

Yggdrasil is managed by the Bot Management Hub at `http://localhost:8420`.

Agents working on Yggdrasil should:
- Direct the operator to the Hub UI for start/stop actions -- the Hub handles all process lifecycle.
- Use the Hub's embedded Terminals tab for all command execution -- no separate PowerShell windows.
- Check the Hub's Diagnostics tab before debugging connection or runtime issues.
- Reference the Hub's Logs tab to tail Yggdrasil server output.
- When runner lease recovery triggers, notify via the Hub's Notification Centre (`POST /api/notifications/add`).
- Never open separate CMD/PowerShell windows -- use the Hub Terminals tab.
- Check the Hub's Game Mode (Ctrl+G) when advising on performance-sensitive or resource-heavy features.
