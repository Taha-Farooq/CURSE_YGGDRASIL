# Programs

This folder contains user-facing runtime programs, separated from automation tooling.

- `game/` -> playable/runtime game program entry points.
- `asset-adder/` -> content/asset ingestion and registration program entry points.

Automation is intentionally separate:
- `bots/`
- `automation/`
- `scripts/`

