# Asset Adder Program

Use this folder for the asset/content ingestion tool that is separate from automation bots.

Current starter command:

```powershell
./launch-asset-adder.ps1
```

Launcher executable:

- `AssetAdderLauncher.exe`

Build/rebuild launcher:

```powershell
./scripts/build-program-exes.ps1
```

Replace placeholder script logic with your actual asset pipeline integration.

