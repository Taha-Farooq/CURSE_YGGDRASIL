# Game Program

Use this folder for the game runtime entrypoint and related launcher scripts.

Current starter command:

```powershell
./launch-game.ps1
```

Launcher executable:

- `GameLauncher.exe`

Build/rebuild launcher:

```powershell
./scripts/build-program-exes.ps1
```

Replace the PowerShell launch script logic with your real engine executable once available.

